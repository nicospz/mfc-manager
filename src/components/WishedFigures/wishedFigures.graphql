query wishedFigures {
    figures(status: WISHED) {
        id
        title
        price
        shop
        status
        releaseDate
        paymentDate
        imageUrl
        wishability
    }
}
