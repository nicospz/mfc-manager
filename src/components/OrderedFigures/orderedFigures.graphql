query orderedFigures {
    figures(status: ORDERED) {
        id
        title
        price
        shop
        status
        releaseDate
        paymentDate
        imageUrl
        score
        wishability
    }
}
